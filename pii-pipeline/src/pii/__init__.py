"""Companion code for the 'Handling PII in Data Pipelines' article series.

Part 1 — detection & classification : generate_data, heuristics, profile_scan,
                                      presidio_scan, validate_contract
Part 2 — choosing a technique        : masking, hashing_demo
Part 3 — the end-to-end pipeline     : pipeline, serve, erasure (+ tests/)
"""
